{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
<div class="flex items-center flex-col w-full rounded-lg bg-darkTransparent p-5 text-white">

    <h1 class="mb-3">Question {{ question.id }}</h1>
    <img width="250" src="{{question.mediaurl | imagine_filter('my_background_filter') }}" class="img-fluid"/>


    <h2 class="my-4">Quel est le nom de cette figure ? </h2>


    {{ include('question/_delete_form.html.twig') }}
</div>
{% endblock %}  