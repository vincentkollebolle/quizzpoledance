{{ form_start(form) }}
<div class="w-full flex flex-col items-center my-5">
    <div class="md-input mb-5">
        {{ form_widget(form.content) }}
        {# {{ form_label(form.content) }} #}
    </div>
    <button class="md-btn btn btn-light">{{ button_label|default('Enregistrer') }}</button>
    <a class="md-btn" href="{{ path('answer_index') }}"><button type="button" class="btn btn-light">Retour à la liste</button></a>

</div>
{{ form_end(form) }}
