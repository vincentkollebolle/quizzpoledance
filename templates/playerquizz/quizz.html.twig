{% extends 'base.html.twig' %}

{% block title %}Quizz Pole Dance{% endblock %}

{% block body %}
    <div class="text-light text-center mx-auto">
    
     <h1 class="text-light">Bienvenue !</h1>
      <h3 class="text-light">
        À tous les fans (ou pas) de Pole Dance,<br />
        testez vos connaissances des figures <br/>
        de ce sport/art, mixant danse et accrobaties <br/>
        avec ce quizz en ligne ! <br />
        Amusez vous ! 
      </h3>


        {% if error != null %}
            <span class="text-danger">{{ error }}</span>
        {% endif %}
        {{ form_start(form) }}
            <div class="w-50 flex flex-col justify-content-center text-center mx-auto">
                <div class="md-input mb-2 mx-auto">
                    {{ form_widget(form.playername) }}
                    {# {{ form_label(form.playername) }} #}
                </div>
                {{ form_widget(form.start, {'attr': {'class':'md-btn col-sm-12 btn btn-light mb-5'}}) }}
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
